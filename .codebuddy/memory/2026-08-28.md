# 2026-08-28 工作日志

## 完成：MaaFramework 生态调研 + 开发规划文档

- 调研了 MaaFramework 及其社区项目（通用 GUI、开发工具、应用程序三大类）。
- 重点梳理了 AI Agent CLI/MCP 项目：maafw-cli、MaaMCP、create-maa-project、Everything-Maa、MaaEvidenceKit、maaframework-skills。
- 调研结论：现有"编辑+运行"GUI 偏开发者向（MaaInspector），不存在"大众向、录制即用、基于 Maa 图像识别"的按键精灵式 GUI —— 确认为项目空白。
- 评估 Rust 可行性：确认存在官方 `maa-framework-rs` 绑定；MaaInspector 已用 Tauri(Rust)+Vue3+vue-flow 同栈验证。结论：Rust 完全可行且最优。
- 产出：`docs/开发规划.md`（v0.1），全量改为 Rust 技术栈（Tauri2 + Vue3 + Vue Flow + maa-framework-rs），含阶段0~5路线图、模块设计、可复用资产、风险合规。
- 同步更新 `MEMORY.md`（项目定位/技术栈/生态事实）。

## 完成：详细功能规格 `docs/spec.md`

- 细化各模块功能清单：M0 运行时封装、M1 图编辑器（完整 maafw 识别/动作类型表 + 使用指引系统）、M2 录制引擎、M3 模板/ROI 抓取、M4 设备管理、M5 调试回显、M6 AI 增强。
- **录制→图编辑器串联**：统一 `PipelineDocument` 共享数据模型；Recorder 作为节点生成器，录完即写入模型并由编辑器直接渲染（录完即得 pipeline）。
- **智能识别**：录制点击→自动裁剪 ROI 存模板→生成 TemplateMatch+Click；对 ROI 尝试 OCR→命中则改 OCR 节点；规避裸坐标（坐标模式仅作兜底）。
- **图编辑器完整 maafw 支持**：列出全部 Recognition（DirectHit/TemplateMatch/FeatureMatch/ColorMatch/OCR/NNClassify/NNDetect/And/Or/Custom）与 Action（含 Command/Shell/Custom 等），含参数效果说明与使用指引（含框选 ROI、模板导出、OCR 回填、上下文提示）。
- **反馈迭代循环**：maafw-cli/MaaMCP 作为 AI↔设备的现成通道；构建 生成→运行→观测→分析→修正→沉淀 闭环。

## 完成：调研 MaaFramework 官方推荐 GUI 框架并写入文档

- 结论：MaaFramework 集成文档(2.1)未点名 GUI，但**事实上的官方推荐 GUI 是 MFAAvalonia**（`MaaXYZ` 官方组织、README 通用 UI 首位、Avalonia UI + SukiUI、消费 Project Interface V2）。
- 更本质的官方推荐：**集成方案2（JSON + 插件扩展/AgentServer 子进程）** + **Project Interface V2 协议**（GUI/资源解耦标准）。
- 本项目选型决策：保留 Rust+Tauri（既有决策），**采纳 PI V2 协议**使产出资源包与 MFAAvalonia 等官方 GUI 互通；MFAAvalonia 作参考实现/兼容目标，不直接套用（C# 冲突）。
- 已更新 `docs/开发规划.md` §2.4、§7 与 `docs/spec.md` M0（PI V2 项目接口 + 集成方案2）。

## 完成：grill-with-docs 审查 + 测试计划（用户调用 skill）

- **修正 spec/规划 中 3 处事实性错误**（已核实 MaaFramework 源码/文档）：
  1. `Click{target:"self"}` → 非既有语法；动作默认作用于识别命中区（hitbox），无需显式 target。
  2. 滑动误用 `TemplateMatch + Swipe` → 应为坐标 `begin/end` + `Swipe{begin,end,duration}`。
  3. `[JumpBack]` 误建模为节点字段 → 实为 `next`/`on_error` 列表中的特殊标记字符串；`is_sub` 已废弃。
  4. `enigo` 误用于执行 → 实际执行由 MaaFramework Controller 完成，enigo 非必需（spec M2 行 + 规划 §2.1 已修正）。
- **新增 ADR（docs/adr/0001~0004）**：0001 Rust+Tauri 选型(非MFAAvalonia)、0002 PipelineDocument 后端为唯一真相源、0003 ROI/分辨率靠"重识别"抗变(非坐标缩放)、0004 AI 能力走外部子进程不内置 Python。
- **新增术语表 docs/CONTEXT.md**：统一 Pipeline/Node/Recognition/Action/Controller/Resource/Tasker/PI V2/ROI/Template/PipelineDocument/[JumpBack]/AgentServer/Element 等术语与歧义标记。
- **新增 docs/test-plan.md**：单元(Rust)/集成/E2E 分层测试 + 夹具 + 覆盖门禁；含 V1/V2 round-trip、录制 step→node、识别优化 pass、PI V2 互通等用例。
- 仍存开放歧义（待实现时确认）：桌面端仅 Windows（Win32 控制器非跨平台）、AI 阶段 Python 环境探测、录制时如何判定"点击落在文本框"。

## 完成：阶段0 工程搭建（用户指令"开始搭建"）

- 安装 Windows 原生 Rust（winget Rustlang.Rustup），rustc 1.98.0 msvc；WSL 里的 Rust 不可用于本项目（需 Win32 控制器）。
- 核实 maa-framework v1.23.0 真实 API（docs.rs）：load_library / Toolkit::init_option+find_adb_devices / Controller::new_adb+new_win32 / Resource::post_bundle / Tasker::bind+post_task(返回 TaskJob 需 wait)。Controller/Tasker 均 Send+Sync，可存 Tauri 状态。
- 搭建 Tauri2 + Vue3 + Vite 工程：前端面板（加载库/设备连接/加载资源/运行任务/日志）、Rust 侧 M0 运行时封装 `src-tauri/src/maa/runtime.rs` + 命令层 `commands.rs`、示例资源 `resource/pipeline/demo.json`、Makefile、SDK 下载脚本。
- **验证结果**：`cargo check` 零错误零警告、`cargo build` 成功（MSVC 链接 OK）、`npm run build` 成功；SDK 已下载到 maa-sdk（v5.12.3）。
- 关键实现点：`#[cfg(feature="dynamic")]` 对本 crate 无效（feature 属依赖），故直接无条件调用 load_library；用 spawn_blocking 跑任务避免冻结 UI；新增 resolve_existing_path 解决 dev 期 cwd 差异。
- 踩坑见 MEMORY.md（图标 RC.EXE、PS 中文 BOM、esbuild postinstall）。

## 完成：阶段1~5 全部实现（用户指令"继续做完剩下的所有阶段"）

- **阶段1 图编辑器**：`pipeline/model.rs` 手写 V1/V2 序列化（V1 扁平字段需按 RECOGNITION_FIELDS/ACTION_FIELDS/NODE_FIELDS 判别归属，未知字段进 extra 保证 round-trip）；`PipelineState` 持有文档(ADR 0002)；前端 Vue Flow 分层布局。
- **阶段2/3 录制引擎**：inputbot 监听（LeftButton 按下后轮询 is_pressed 判定点击 vs 拖拽；KeybdKey::iter 遍历绑定全部按键，可打印字符累积合并为 InputText）；scrap 截屏 → crop_roi 裁剪存模板 → 生成 TemplateMatch+Click 节点；坐标模式兜底生成 DirectHit+roi。
- **阶段4 设备管理+调试**：`Toolkit::find_desktop_windows()`（DesktopWindow{hwnd,class_name,window_name}）；`tasker.add_sink` 转发 "maa://event" 到前端做节点高亮。
- **阶段5 AI 增强**：`ai/` 探测 python/uv/uvx，Command 子进程运行 maafw-cli/MaaMCP，按 ADR 0004 不打包 Python、缺失时优雅降级。
- **M3 ROI**：`capture_grab_template`/`capture_screenshot` 命令 + `RoiCapture.vue` 拖拽框选（按 naturalWidth/clientWidth 换算缩放），抓取后自动把选中节点改为 TemplateMatch。
- **验证**：`cargo check` 零错误零警告；`npm run build` 38 模块通过。
- 编译坑：inputbot `bind` 要求 `Fn`（闭包内 Arc 需先 clone 再移入线程）；`parse_spec` 被识别/动作共用须返回元组而非具体结构体；strum 需与 inputbot 同版本(0.25)才能 `KeybdKey::iter()`。
- 已知未做：录制期窗口枚举未接 Toolk 到前端(已有命令)、无单元测试落地（test-plan.md 已规划）、端到端需真实设备验证。

## 完成：全部 lint 清零 + 单元测试落地 + 推送 GitHub

- **lint 修复**：clippy 3 处（derive Default、struct update 替代 field_reassign、? 运算符）、vue-tsc 1 处（GraphEditor edges 类型化 Edge[]）、markdownlint 5 处（ASCII 图加 ```text、删孤立围栏、blockquote 空行合并）。`.markdownlint-cli2.jsonc` 配置忽略 `maa-sdk/`（第三方 SDK 文档不改）。
- **tooling 固化**：`npm run lint`（lint:md/lint:ts/lint:rust）、`npm run test`（cargo test）；Makefile 新增 `make lint`/`make test`。markdownlint-cli2 加入 devDependencies。
- **单元测试 25 个全绿**（`cargo test`）：model.rs（V1/V2 round-trip、extra 保真、$保留键、next 对象形式、版本解析）、recorder（steps_to_nodes：TemplateMatch/DirectHit/文本合并/Swipe/ClickKey、key_to_char/key_to_vk 映射）、capture（crop_roi 中心/边界钳制/超界、crop_rect 越界/负坐标、save_template 建目录）。
- **git**：`git init` → main 分支，提交 49 文件 1.4 万行；`.gitignore` 排除 node_modules/dist/target/gen/maa-sdk/.codebuddy。**已推送 <https://github.com/schalkiii/MaaWizard>**（用 `-c user.name=schalkiii -c user.email=schalkiii@users.noreply.github.com` 一次性提交身份，未改全局 git config）。
- 提交身份约定：GitHub 无 URL 用户名邮箱时用 `<username>@users.noreply.github.com` + `git -c` 一次性指定，避免改全局 config。

## 完成：handoff 交接 + memory 入库
- 撰写 `docs/handoff-2026-08-28.md`（同内容副本在系统临时目录 `%TEMP%/MaaWizard-handoff-2026-08-28.md`）。
- `.gitignore` 改为仅放行 `.codebuddy/memory/`（`!.codebuddy/memory/`），memory 已纳入 git 跟踪并推送（commit 993000a）。
- 推送身份约定照旧：`git -c` 一次性指定，不改全局 config。
