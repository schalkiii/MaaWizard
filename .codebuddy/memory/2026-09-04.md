# 2026-09-04

## markdownlint：memory 报错 + ignores 配置修复

- memory 两个 dated 文件（2026-09-01 / 2026-09-03）每个 `##` 标题后缺空行，触发 MD022 + MD032，共 10 处；补空行后 memory 三文件 lint 归零。MEMORY.md 本身 0 问题。
- 全量 lint 一度淹没在 `tools/MaaPipelineEditor/Editor/node_modules` 的第三方报错里：根因是 `.markdownlint-cli2.jsonc` 的 `ignores` 用了根锚定写法（`node_modules/**`），匹配不到嵌套目录。已改为 `**/` 前缀，并把 vendored 的 `tools/MaaPipelineEditor/**` 整体排除。现在全量第一方文档 17 个 md 全部 0 issues。
- 注意 `globs:["**/*.md"]` 默认不跨点目录，`.codebuddy/memory/*.md` 不会被 `make lint` 扫到（靠编辑器插件单独 lint）。

## MPE 改为 git subtree + fork（用户选定方案 B）

- fork `kqcoxn/MaaPipelineEditor` → `schalkiii/MaaPipelineEditor`，建 `maawizard` 分支 = 上游 `main`(9bf67f37) + 本地 3 个 patch，提交 `37e128b5`(+41/-8)，已推送。
- MaaWizard 侧 `git subtree add --prefix=tools/MaaPipelineEditor mpe maawizard --squash`，纳入 1183 文件，HEAD `1ce34224`。MPE 源码首次正式进入版本库。
- 关键坑：本地旧拷贝基于较老上游，整目录覆盖最新上游会产生 119 文件 / 上万行的「回退上游进展」diff。正确做法是先确认自己的 patch 文件、只提交那几个（本例恰好上游没动过这 3 个文件，所以这 3 个文件的 diff 就是纯 patch，直接提交即可）。
- subtree 要求工作区干净，用 `git stash` / `git stash pop` 保护了用户未提交的改动（AGENTS.md/README.md/src-tauri/*/src_App.vue 等），已确认完整还原。
- AGENTS.md 已同步：新增「MPE 同步与 patch 管理」小节，旧的「该克隆已去 .git」说法已改写。

## 待确认 / 遗留

- 清理候选已处理：`.mpe-tmp/`（fork 临时克隆）与 `tools/MaaPipelineEditor._local_backup/`（旧本地拷贝）已删除。
- `public/mpe` 已从 subtree 源码（上游 9bf67f37 + 本地 3 patch）重构建：`npm install` → `npm run build -- --mode mpe`（base `/mpe/`、输出 `dist/`）→ 拷到 `public/mpe`，共 411 文件、assets 完整、base 路径校验为 `/mpe/`。markdownlint 全量第一方文档 18 md / 0 issues。
- `newcomerStore.test.ts`（为 `skip()` 写的回归测试）仍位于 `tools/MaaPipelineEditor/Editor/src/stores/ui/`，未跟踪，是否提交到 fork 分支待定。
- git status 里 `M tools/MaaPipelineEditor/Editor/yarn.lock` 是 autocrlf（LF↔CRLF）假改动（与之前 Cargo.toml 同性质），非真实修改，勿提交；`tools/MaaPipelineEditor/Editor/package-lock.json` 是本次 npm install 生成的（上游用 yarn），已加入 `.gitignore`。

## 未跟踪文件收敛（417 → 5）

- `public/mpe/`（411 文件 / 25.43 MB）是 MPE 构建产物。subtree 落地后源码已在库内，产物变成可重建的衍生品，故 gitignore（**ignore 不删文件**，磁盘仍在、应用照常运行）。
- 配套防坑：Makefile 新增 `make mpe`（npm install → `npm run build -- --mode mpe` → robocopy /MIR 到 `public/mpe`，/MIR 顺带清掉陈旧文件；不用 Copy-Item，后者遇目录会报 "Container cannot be copied onto existing leaf item" 并静默漏拷 assets），`make distclean` 增加清理 `public\mpe`；AGENTS.md 同步说明。
- 剩余 5 个未跟踪均为真实工作、待用户决定是否提交：3 个 memory 日志、`src-tauri/src/mpe_bridge.rs`、`newcomerStore.test.ts`。
